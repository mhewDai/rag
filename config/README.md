# Configuration Files

This directory contains configuration examples for the RAG system. Copy `config.example.json` to `config.json` and `.env.example` to `.env` to customize settings. Dependencies are managed via `pyproject.toml` (the canonical dependency file).
